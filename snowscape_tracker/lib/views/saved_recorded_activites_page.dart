import 'package:flutter/material.dart';

class SavedRecordedActivitesPage extends StatelessWidget {
  const SavedRecordedActivitesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Saved Recorded Activities Page'),
    );
  }
}
